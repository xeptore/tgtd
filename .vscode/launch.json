{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Launch",
      "type": "go",
      "request": "launch",
      "mode": "auto",
      "cwd": "${workspaceFolder}",
      "program": "${workspaceFolder}/cmd",
      "envFile": "${workspaceFolder}/.env",
      "args": ["--config", "./config.yml"]
    }
  ]
}
